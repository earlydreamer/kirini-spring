# 환경 변수 샘플 파일
# 실제 사용 시 이 파일을 .env로 복사하고 실제 값으로 변경하세요
# 예: cp .env.example .env

# === 공용 데이터베이스 설정 (접두사 없음) ===
# devtest와 prod에서 공통으로 사용하는 기본 설정
HOST=localhost
PORT=3306
DATABASE_NAME=mydatabase
USERNAME=myuser
PASSWORD=your_password_here

# === Dev 배포 환경 전용 설정 (DEV_ 접두사) ===
# devtest 프로파일에서 사용
DEV_HOST=dev-db-host
DEV_PORT=3306
DEV_DATABASE_NAME=dev_database
DEV_USERNAME=dev_user
DEV_PASSWORD=your_dev_password_here

# === Prod 환경 전용 설정 (PROD_ 접두사) ===
# prod 프로파일에서 사용, 운영 DB 정보로 변경 필수
PROD_HOST=prod-db-host
PROD_PORT=3306
PROD_DATABASE_NAME=production_db
PROD_USERNAME=prod_user
PROD_PASSWORD=your_production_password_here

# === 기타 민감 정보 (공용) ===
# JWT Secret, API Key 등 추가 가능
# JWT_SECRET=your_jwt_secret_here
# API_KEY=your_api_key_here

